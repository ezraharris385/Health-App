# Required for the AI assistants (each segment agent + the master agent).
# The rest of the app (logging, charts, scores) works without it.
ANTHROPIC_API_KEY=sk-ant-...

# Optional overrides
PORT=3001
# Model used by all agents
AGENT_MODEL=claude-opus-4-8
